//..で1つ上の階層 VSコードでは見ている階層によって赤線が出るかもしれないが問題ない
import RandomGeneratorView
  from "../features/random-generator/components/RandomGeneratorView";

export default function RandomGenerator () {

return (
    <RandomGeneratorView />
  );
}